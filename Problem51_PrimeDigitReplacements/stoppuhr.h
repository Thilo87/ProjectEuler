#include <ctime>

bool const START = true ;
bool const STOPP = false ;

// gibt die Zeit in Sekunden zurück, die zwischen startUhr( START ) und startUhr( STOPP ) vergangen ist
double stoppUhr( bool start ) ;